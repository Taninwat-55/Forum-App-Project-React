// const express = require('express');
// const router = express.Router();
// const replyController = require('../controllers/replyController');

// // Get all replies for a thread
// router.get('/thread/:threadId', replyController.getRepliesByThreadId);

// // Create new reply
// router.post('/thread/:threadId', replyController.createReply);

// // Update reply
// router.put('/:id', replyController.updateReply);

// // Delete reply
// router.delete('/:id', replyController.deleteReply);

// module.exports = router;